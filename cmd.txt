
clone:

git clone --branch main --no-checkout --filter=tree:0 https://github.com/jurakovic/runtime.git
cd runtime
git sparse-checkout set docs
git checkout main
git checkout docs-init

=============

#mkdocs:
#
#cd docs/design/coreclr
#
#mkdocs build --site_dir 'botr/site'
#
#mkdocs serve -a 0.0.0.0:9902
#
#http://localhost:9902



docker rm -f botr

docker run -d --restart always -p 9903:80 --name botr -v ./docs:/usr/share/nginx/html nginx

http://localhost:9903


==============

rebase upstream:

git remote add upstream https://github.com/dotnet/runtime.git

git fetch upstream main
git checkout main
git rebase upstream/main

git push

========


git log -n 1 --format="%h" --abbrev=40 -- docs/design/coreclr/botr


git checkout --orphan botr

